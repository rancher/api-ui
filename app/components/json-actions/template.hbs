{{#json-collapsible
  open='{'
  close='}'
  length=length
  expanded=true
  as |block|
}}
  {{#if (eq block "summary")}}
    {{length}} actions
  {{else}}
    {{#each-in model as |k v|}}
      <a class="action" {{action "generate" v}}>{{k}}</a>
    {{/each-in}}
  {{/if}}
{{/json-collapsible}}

